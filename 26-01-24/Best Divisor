import math
import os
import random
import re
import sys



if __name__ == '__main__':
    n = int(input().strip())
    div = [i for i in range(1,n+1) if n%i==0]
    print(max(div,key=lambda x: sum(int(i) for i in str(x))))
